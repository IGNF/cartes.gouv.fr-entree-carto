import * as magic_string from 'magic-string';
import { ResolvedConfig, HtmlTagDescriptor, PluginOption } from 'vite';

interface Options {
    /**
     * Name of the global variable that will be used to configure your env variables.
     * In the browser, the default global variable name is `window.__PRODUCTION__APP__CONF__`.
     * @default __PRODUCTION__APP__CONF__
     */
    name?: string;
    /**
     * Name of the configuration file that will be generated.
     * @default config.js
     */
    filename?: string;
    /**
     * Match variable to be configured by `minimatch`.
     * Default value is according to `vite.config` - `envPrefix`.
     * @default 'VITE_*'
     */
    include?: string | string[];
    /**
     * Match variable to NOT be configured by `minimatch`.
     */
    exclude?: string | string[];
}

declare function createContext(viteConfig?: ResolvedConfig, rawOptions?: Options): {
    transform: (code: string, { includeList, excludeList, globName, }?: {
        includeList?: string[] | undefined;
        excludeList?: string[] | undefined;
        globName?: string | undefined;
    }) => {
        code: string;
        map: magic_string.SourceMap;
    };
    transformIndexHtml: () => HtmlTagDescriptor[];
    runBuildConfig: (envConfig?: Record<string, string>, globName?: string, configFilename?: string) => void;
};
declare function viteEnvRuntimePlugin(options?: Options): PluginOption;

export { createContext, viteEnvRuntimePlugin as default };
