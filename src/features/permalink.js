/**
 * Lecture d'un permalien
 * 
 * @param {*} url - permalien
 * @returns {Array} Liste d'options de couches
 */
export const getInfoFromPermalink = (url) => {
  throw new Error(`Le permalien n'est pas implementé !`);
};